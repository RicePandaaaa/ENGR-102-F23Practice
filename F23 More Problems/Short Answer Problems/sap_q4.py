"""
Lists store items and they are indexed by integers implicitly (not defined by the programmer). Dictionaries store key-value pairs and values are only accessed with keys. Furthermore, both the keys and values must be provided by the programmer.
"""